@model IEnumerable<FialBP.Models.BusType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BTName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BTName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BTypeId })
        </td>
    </tr>
}

</table>
